import React from 'react';

const LightActionButtons = ({ light }) => (
  <div className="c-action-buttons c-light-action-buttons">
    (light actions)
  </div>
);

export default LightActionButtons;
