import React from 'react';

const StateActionButtons = ({ state }) => (
  <div className="c-action-buttons c-state-action-buttons">
    (state actions)
  </div>
);

export default StateActionButtons;
